services:
  web:
    build: .
    ports:
      - "5050:5050"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./static:/app/static
      - ./web_server.py:/app/web_server.py
    command: python -u web_server.py
